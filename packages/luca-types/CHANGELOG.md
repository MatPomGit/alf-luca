# Changelog luca-types

Ten plik dokumentuje zmiany zgodnie z **SemVer** dla modułu `luca-types`.

## [Unreleased]
### Migration notes
- Import legacy `luca_tracker.types` migruje do `luca_types.types`.
- Dla `config_model` należy używać `luca_types.config_model`; mapowania wejść/wyjść są utrzymywane w `luca_input.config_mapping`.

## [0.1.0] - 2026-04-10
### Added
- Inicjalna wersja modułu przygotowana do niezależnego wydawania i publikacji.
